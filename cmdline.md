Organize your tabs on iTerm2 by setting colors and titles with a command line command. Use `tabset` to set the tab name and it will color the tab header as well.

```
npm install -g iterm2-tab-set
```
